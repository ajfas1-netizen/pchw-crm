# Striking Details — Book

Phone-first CRM and ledger for Striking Details. One HTML file, no build step, works offline.

**No business data lives in this repo.** Everything is stored on the phone,
encrypted with the user's passcode (PBKDF2 + AES-GCM) in IndexedDB. This repo is
only the app shell. Back up from More → Export.
